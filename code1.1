#include <iostream>
#include <conio.h>
using namespace std;

int main()
{
	setlocale(LC_ALL , "Russian");
	double item[5];
	int i=1;											
	double price = 0;   
	double money,change;
		item[0] = 3;
		item[1] = 6,50;
		item[2] = 9;
		item[3] = 14,25;
		item[4] = 26;
	cout << "Choose items what you want to buy:" << endl;
	cout <<"1. Milk\n";
	cout <<"2. Chocolade\n" ;
	cout <<"3. CocaCola\n";
	cout <<"4. CandyPack\n";
	cout <<"5. Smartphone\n";
	cout <<"If you want to end - press '0'\n";
	while (!(i == 0))									
	{
		cin >> i; 										
		if (i > 0 && i < 6)
		{
			price = price + item[i-1];											
		} else
		{
			if (i == 0) break;
			cout << "Uncorrect choose\n";
		}

	}	
	cout << "You must pay: "<< price << "$" << endl;
	cout << "Put your money here: ";
	while (1)
	{
		cin >> money;
		if (money <= 0)
		{
			cout << "Incorrect enter, please try again\n";
		}
		else 
		{
		if (money >0 && money < price)
		{
		cout << "You must add " << price - money << "$";
		}
		else break;
		}
	}
	change = money - price;
	cout << "Your change: " << change << "$";


	
	_getch();
	return 0;
}
