#include <iostream>
#include <conio.h>
using namespace std;

int main()
{
	setlocale(LC_ALL , "Russian");
	double item[5];
	item[0] = 3;
	item[1] = 6,50;
	item[2] = 9;
	item[3] = 14,25;
	item[4] = 26;
	//char quit = ' '; 										//Коли в циклі quit буде дорівнювати - q => тоді цикл вибору продуктів має закінчитись
	int i=1;												//Змінна якою я вибираю продукти в циклі
	double price = 0;   										//Загальна ціна 
	cout << "Choose items what you want to buy\n" << "1. Milk\n" << "2. Chocolade\n" << "3. CocaCola\n" << "4. CandyPack\n" << "5. Smartphone\n";
	while (!(i == 0))										//Поки i не дорівнює 0
	{
		cin >> i; 											//Я вибираю індекс продукта з масива
		if (i > 0 && i < 6)
		{
			price = price + item[i-1];							//Ціна = ціна + item[число яке я вибрав]					
		cout << "Price " << price << endl;
		}
		else 
		{
		break;
		}
	
		//i = 0;											//Обнуляєм зміну з вибіром продукта
		//cin >> quit;										//Программа провіряє чи я не написав q і чи не вийти з циклу
	}	
	cout << "You must pay: "<< price << " dollars" << endl; //Я маю заплатити "ЦІНА"



	
	_getch();
	return 0;
}
